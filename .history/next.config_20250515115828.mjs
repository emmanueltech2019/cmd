/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['']
    }
    images: {

    domains: [''], // add your allowed domains here
  },
};

export default nextConfig;
