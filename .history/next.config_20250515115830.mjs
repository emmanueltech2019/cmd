/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['medellinmd.framer.website']
    }
    images: {

    domains: [''], // add your allowed domains here
  },
};

export default nextConfig;
